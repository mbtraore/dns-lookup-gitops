apiVersion: apps/v1
kind: Deployment
metadata:
  name: dns-lookup
spec:
  template:
    spec:
      containers:
        - name: dns-lookup
          env:
            - name: DOMAINS
              value: "google.com,github.com,seneweb.com,x.com"
            - name: LOOP_INTERVAL
              value: "300"
